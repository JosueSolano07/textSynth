class ConversationSummarizer:

    async def summarize(
        self,
        messages: list
    ) -> str:

        return ""

    async def compress(
        self,
        session_id: str
    ):
        pass